# HW1: Предсказание стоимости автомобиля 🚗

В этом репозитории содержится решение первого домашнего задания по классическому машинному обучению (курс ML 2022).

## 📄 Содержание
* `HW1_Car_Price_Prediction.ipynb` — Jupyter-ноутбук с полным циклом анализа и обучения (EDA, Preprocessing, Modeling).
* `app.py` — интерактивное веб-приложение на Streamlit.
* `model_pack.pkl` — сериализованная модель и объекты препроцессинга.

## 🛠 Что было сделано

1. **EDA (Разведочный анализ данных):**
    * Проанализированы типы данных, найдены и заполнены пропуски (медианами).
    * Удалены явные дубликаты.
    * Из строковых признаков (`mileage`, `engine`, `max_power`) извлечены числовые значения, удалены единицы измерения.
    * Построены визуализации: попарные распределения (pairplot), тепловая карта корреляций, гистограммы.

2. **Моделирование:**
    * Обучена базовая **LinearRegression** только на вещественных признаках ($R^2 \approx 0.59$).
    * Применена стандартизация (`StandardScaler`) — метрики не изменились, но веса стали интерпретируемыми.
    * Обучены **Lasso** и **ElasticNet** с подбором гиперпараметров через `GridSearchCV`.
    * Обучена **Ridge** регрессия с добавлением категориальных признаков через `OneHotEncoding`.

3. **Разработка сервиса:**
    * Создано приложение на Streamlit.
    * Реализована загрузка данных (CSV) или ручной ввод параметров.
    * Добавлена визуализация влияния признаков на цену.

## 📊 Результаты

Лучшей моделью оказалась **Ridge Regression** с закодированными категориальными признаками (OneHotEncoding).

| Модель | R2 (Test) | MSE (Test) | Бизнес-метрика (% попаданий в 10%) |
|:---|:---:|:---:|:---:|
| Linear Regression (base) | 0.5941 | 2.33e11 | 22.7% |
| Lasso (tuned) | 0.5930 | 2.34e11 | 22.5% |
| **Ridge + OHE (Best)** | **0.5989** | **2.30e11** | **26.6%** |

**Наибольший буст качества** дало добавление категориальных признаков (`fuel`, `transmission`, `seller_type`). Это позволило поднять бизнес-метрику с 22.7% до 26.6%, а также значительно улучшило качество на тренировочной выборке.

## 📉 Что не удалось
1. **L1-регуляризация (Lasso):** При дефолтных параметрах Lasso не занулила ни один признак. Даже при `alpha=1000` отбора признаков не произошло. Это связано с огромным масштабом целевой переменной (цены в миллионах).
2. **Сильно улучшить R2:** Метрика на тесте застряла в районе 0.60. Вероятно, зависимость цены от признаков не совсем линейная, и классические линейные модели достигли своего потолка. Требуется использование более сложной модели или генерация полиномиальных признаков.

## 📱 Оценка сервиса

Приложение доступно по ссылке: [вставить ссылку на Streamlit Cloud после деплоя]

**Плюсы:**
* Удобная лента графиков EDA (можно добавлять нужные визуализации по клику).
* Возможность загрузить CSV-файл и получить предсказания сразу для пакета данных.
* Наглядная интерпретация весов модели.

**Минусы/Ограничения:**
* При ручном вводе нужно заполнять много полей.
* Модель чувствительна к выбросам во входных данных (так как это линейная регрессия).

**Планы по улучшению:**
* Добавить автоматический парсинг единиц измерения при вводе (чтобы пользователь мог писать "20 kmpl").
* Заменить линейную модель на CatBoost для повышения точности.